apiVersion: v1
kind: Service
metadata:
  name: {{ include "p2-nodejs-app.fullname" . }}
  labels:
    {{- include "p2-nodejs-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "p2-nodejs-app.selectorLabels" . | nindent 4 }}
