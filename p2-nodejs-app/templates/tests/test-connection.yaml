apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "p2-nodejs-app.fullname" . }}-test-connection"
  labels:
    {{- include "p2-nodejs-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "p2-nodejs-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
